# === Long-form video: ~10 min, 480p@30fps, auto-generated switching backgrounds ===
# DeepSeek 寫多分鏡腳本 → 每段 TTS(台灣腔) + 自動生成對應背景圖(Pollinations) →
# 合成 480p@30fps，背景隨腳本切換、燒上繁體字幕。產出為 artifact 下載。
name: Long-form Video

on:
  workflow_dispatch:
    inputs:
      topic:
        description: '主題（留空 = LLM 自動產生）'
        required: false
        type: string
      minutes:
        description: '影片長度（分鐘）'
        required: false
        type: string
        default: '10'
      voice:
        description: 'TTS 語音'
        required: false
        type: string
        default: 'zh-TW-HsiaoChenNeural'

jobs:
  longform:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      - name: Install ffmpeg + CJK fonts
        run: sudo apt-get update && sudo apt-get install -y ffmpeg fonts-noto-cjk fontconfig && sudo fc-cache -f
      - name: Install deps
        run: pip install -r requirements.txt

      - name: Connect to Tailscale (reach local vLLM; falls back to DeepSeek)
        continue-on-error: true
        uses: tailscale/github-action@v3
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
          tags: tag:ci

      - name: 1) Generate long-form script (DeepSeek)
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: python scripts/generate_longform.py --topic "${{ inputs.topic }}" --minutes ${{ inputs.minutes || '10' }} --output scenes.json

      - name: 2) Per-scene TTS + photorealistic background images (1080p)
        run: python scripts/generate_scene_assets.py --scenes scenes.json --voice "${{ inputs.voice || 'zh-TW-HsiaoChenNeural' }}" --workdir build --width 1920 --height 1080

      - name: 3) Assemble 1080p@30fps (transitions + subtitles + audio loudnorm)
        run: python scripts/assemble_longform.py --manifest build/manifest.json --output output/final_video.mp4 --fps 30 --transition 0.8 --font "Noto Sans CJK TC"

      - name: 3b) Generate thumbnail
        run: python scripts/make_thumbnail.py --image build/img_00.jpg --title "$(cat title.txt)" --output output/thumbnail.jpg --font "Noto Sans CJK TC"

      - name: Upload video
        uses: actions/upload-artifact@v4
        with:
          name: longform-video
          path: |
            output/final_video.mp4
            output/thumbnail.jpg
            scenes.json
            title.txt
            description.txt
            tags.txt
          retention-days: 7
